apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: demo-app
  namespace: demo
  labels:
    release: kube-prom-stack
spec:
  selector:
    matchLabels:
      app: demo-app
  endpoints:
    - port: http
      interval: 10s
